# Agentflow customer export — after

The application generated by the recorded Agentflow run, with its real scorecards and complete evidence. **12/12 product tests and 29/29 independent checks passed; both LLM judges scored 1.00.** One cycle, zero supervisor interventions, 13m 13s including delivery.

You can [read the results](results/README.md), [inspect the scorecard](results/operator/scorecard.json), or [open the review brief](results/run/delivery/01-review-brief.md) directly on GitHub. Running this dashboard or checking the result requires **no Agentflow installation, Codex account, API key, or AI run**. To execute the workflow from scratch, use the [before repository's full setup guide](https://github.com/koji98/agentflow-customer-export-before#3-install-agentflow-and-codex-for-an-ai-run).

## Prerequisites

These commands use bash/zsh on macOS or Linux. On Windows, use WSL2; native PowerShell setup is not covered.

| Tool | Version / purpose | Install help |
| --- | --- | --- |
| Git | Clone the repositories | [Git downloads](https://git-scm.com/downloads) |
| Node.js + npm | Node **24.18.0**, pinned in `.nvmrc`; npm is bundled | [Install nvm](https://github.com/nvm-sh/nvm#installing-and-updating), then use the commands below |
| Python | **3.10+**, available as `python3`, for acceptance checks | [Python downloads](https://www.python.org/downloads/) |

The app has no npm or pip dependencies, build step, database, Docker requirement, or `.env` file. `npm ci` verifies the committed package metadata and Node requirement. `.npmrc` rejects unsupported Node versions; Node 24.x is supported, with 24.18.0 used for the recorded run. Python 3.12 was used for that run.

## 1. Clone and start the resulting app

Install the prerequisites above first. Then run:

```sh
git clone https://github.com/koji98/agentflow-customer-export-after.git
cd agentflow-customer-export-after
nvm install
nvm use
npm ci
npm run doctor
PORT=4318 npm start
```

The doctor should end with `Environment checks passed`. The server should print `Northstar ready at http://127.0.0.1:4318`. Open that URL. Port 4318 lets this run beside the before app on 4317. Press Ctrl+C to stop the server; run `nvm use` in every new terminal.

Select Active, move to page two, and click Preview export. The preview describes all 103 matching customers, displays a five-record sample, and downloads the complete matching CSV.

**Recorded UI issue:** the preview opens below the directory without scrolling into view. Scroll down to inspect it. This is preserved from the actual run, not silently repaired after scoring. See the [independent browser review](results/operator/browser-review.md).

## 2. Verify the result

From this repository root:

```sh
npm test
npm run check:acceptance
npm run test:setup
```

Expected: **12 product tests**, **29 independent acceptance cases**, and **four setup-doctor tests** pass. The doctor and these checks start local servers but do not call an AI service. Test fixtures use synthetic customer data.

To inspect the exact recorded application changes:

```sh
git diff before-agentflow..after-agentflow -- src public test
```

The tags freeze the original before and after snapshots. Later main-branch commits improve setup only. The application source, UI, data, product tests, and archived run evidence remain unchanged.

## 3. Walk through what happened

1. [Implementation summary](results/operator/implementation-summary.md): what changed and what was validated.
2. [Actual scorecard](results/operator/scorecard.json): deterministic result and both judges' explanations.
3. [Command output](results/operator/deterministic-checks.log): the original test evidence.
4. [Review brief](results/run/delivery/01-review-brief.md), [run learnings](results/run/delivery/02-run-learnings.md), and [event stream](results/run/events.jsonl).
5. [Browser review](results/operator/browser-review.md): the visibility finding outside the source-only rubric's coverage.

The archive contains all **202 recorded run files** and **28 operator-evidence files**. [Provenance](results/PROVENANCE.md) explains normalized machine paths and unchanged scores/timestamps; the [archive manifest](results/archive-manifest.json) records original and exported hashes. Archived runtime files are a historical record, not a relocated resumable run.

## Setup troubleshooting

| Symptom | Fix |
| --- | --- |
| `nvm: command not found` | Install nvm using the link above, reopen the terminal, then run `nvm install` and `nvm use` in this repo. |
| `EBADENGINE`, or the doctor reports Node 20/22 | Run `nvm use` in this shell. Run it again in each new terminal. |
| `python3` missing or too old | Install Python 3.10+ and verify `python3 --version`. No `pip install` step is needed. |
| `EADDRINUSE` | Another app owns the port. Stop it, or use `PORT=4321 npm start` and open the URL printed by the server. |
| `EPERM` / `EACCES` binding `127.0.0.1` | Use a local terminal or runner that allows localhost servers. The doctor checks this without running a model. |

`npm run test:setup` tests the doctor's missing-tool, old-Python, and signed-out cases. GitHub Actions checks setup and product behavior on Linux and macOS without an AI account or secrets.

## Repository map

| Path | Contents |
| --- | --- |
| `src/`, `public/`, `data/`, `test/` | The scored application and product tests |
| `agentflow.graph.json` | Portable graph; execute it from the before environment for a fresh demo |
| `TICKET.md`, `EXPORT_PREVIEW.md`, `APP_GUIDE.md` | Original task inputs |
| `showcase/acceptance/` | Independent checks and fixture |
| `showcase/setup/` | Environment doctor and setup tests |
| `results/run/` | Prompts, responses, events, attempts, checks, artifacts, delivery, and patches |
| `results/operator/` | Saved scorecard, test output, preflight, integrity, timing, and browser review |

This successful first cycle does not demonstrate a repair loop. Use the saved evidence during a live presentation; the measured run exceeded an eight-minute demo slot.
